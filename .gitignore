# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# Environment variables (runtime - never commit)
.env
.env.local
.env.*.local
.env.production
.env.staging

# Docker Compose overrides (auto-generated - never commit)
docker-compose.override.yml

# Kubernetes secrets (runtime - never commit)  
secrets.yaml
*-secrets.yaml

# Note: Template files are safe to commit:
# .env.template - documentation template
# .env.docker - safe development values
# secrets.template.yaml - K8s structure template

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Project specification documents (keep private)
requirements.md
design.md
tasks.md
CLAUDE.md

# Logs
*.log

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Build artifacts
bin/
dist/
build/

# Kubernetes secrets
*.key
*.crt
secrets/
